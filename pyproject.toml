[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gpt"
version = "1.0.0"
dependencies = ["typer", "rich", "questionary"]

[project.scripts]
gpt = "main:app"